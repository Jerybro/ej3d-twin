# Python
__pycache__/
*.pyc
.venv/

# NDA 防呆：客戶原始數據絕不進版控（models/ 只放訓練產物權重）
提供資料*/
dataset/
*.xls
*.xlsx
quality_daily*.csv
uploads/
# P&ID 解析衍生場景含客戶位號，不進版控
data/scenes/pid-*.json
data/pid_batch_status.json
# 圖組是伺服器執行期會寫入的狀態檔：進版控會讓部署機工作樹永遠是髒的，
# 擋掉自動部署（deploy/auto-pull.ps1 的 dirty 防呆）
data/pid_groups.json

# 自動部署紀錄
deploy/auto-pull.log

# 產出物（可由程式重新生成）
exports/*.usda

# 掃描重建模型與示範資產（檔案過大，不進版控；跑 tools/fetch_demo_assets.py 補齊）
scans/*
!scans/.gitkeep

# 保留資料夾結構
!exports/.gitkeep

# IDE / OS
.vscode/
.DS_Store
Thumbs.db
server_8601.log

# 本機開發工具設定
.claude/

# AutoML 模型持久化（含使用者資料衍生）
data/automl/

# �{�Ҿ��һP���_�]�����i�����^
.env
.secret_key

batch_rerun.log


# P&ID �ХܤơG�H�u�е��P�]�֭y��t�Ȥ�츹�A���i����
data/pid_annot/
logs/

# 資產模型與現場評註：伺服器執行期寫入（每次建模／存評註都改寫）。
# 留在版控＝①自動部署每次都撞髒工作樹而跳過 ②模型 JSON 內含整張圖的
# 線段與 OCR 文字，可還原客戶圖面，與 data/pid_annot/ 同一級別的客戶資料。
data/pid_model/
data/pid_notes/

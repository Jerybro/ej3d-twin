# 變更紀錄

平台目前版本寫在 [`VERSION`](VERSION)（單一真相）；伺服器由 `/api/version` 回報、首頁頁尾顯示。
格式依 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版號依語意化版本：
**主版本**＝資料格式或 API 不相容、**次版本**＝新功能、**修訂版**＝修 bug 與文案。
0.x 期間＝功能已完整但尚未對外正式交付，介面仍可能調整。

每個條目寫「使用者看得到的差別」，不寫實作細節（那在 commit 訊息裡）。

---

## [0.9.0] — 2026-08-18

第一個掛版號的版本。以下彙整 2026-08-11 起累積的功能，作為基線。

### 新增
- **P&ID 判讀工作台**：上傳 PDF → 自動辨識（OCR＋幾何＋雲端 VLM）→ 逐項人工審核入庫 → 產出資產模型
- **資產模型比對視圖**：關／並排／疊圖／僅資產模型四種模式；盲重建（純資料庫重畫）／標示／描圖三種內容
- **資產模型可直接編輯**：框選中即長把手可拖拉改大小、移動；箭頭可點選改方向或刪除；兩側同步
- **批次確認設備候選**：清冊已帶位號名稱規格，只需判框對不對；鍵盤 Enter／E／N 快速流
- **重框（L1）**：框不準按 E 重畫，稽核留原框
- **改配對（L2）**：AI 配錯清冊列時直接改指
- **P&ID 圖組**：同一套廠的圖分組，跨圖查找項次號
- **製程順序圖**：設備級流向推導（誰先誰後／分流匯流）；VLM 判方向；人工可改
- **製程說明**：依已確認清單與現場評註生成，逐句標引用來源；產生後存檔不自動重跑；滑過位號在圖上高亮
- **現場評註（RAG）**：對元件或框選區留評註，會影響製程說明並標明出處
- **加強辨識框選區域**：人指哪裡打哪裡，AI 只獵該區域漏掉的元件
- **審核紀錄版本史**：每次寫入留快照，可回到上一動或還原指定版本
- **網域分租**：同公司共用、跨公司互不可見；管理員全視角
- **登入白名單**：未受邀帳號 403；管理員可邀請
- **圖面刪除**：檔案卡右上角 ×，刪 PDF 與快取、保留審核成果
- **首頁「帶我去」**：命中意圖直接跳轉

### 修正
- 並排比對從第一天就沒真正左右並排（CSS 類別撞名），已修
- 候選清單改為對照現在的資產庫過濾：處理過的不再重複出現；早期框在清冊表格上的位號會補上圖面位置候選，接受時改原筆不新增
- 最後一項按「判讀有誤」無反應
- 重建圖去噪：拿掉未審儀錶灰虛圈、拓撲閥件灰蝴蝶結、已審閥件藍圈；候選標籤移到框外
- 聚焦框改聚光燈（框外壓暗），在整排紅框區也分得出正在問哪一個
- 工具列改分段控制元，長檔名省略，不再兩層錯位
- 說詞白話：寫入台帳→存入資產庫、錨定問答→判讀剩餘符號、缺口掃描→加強辨識框選區域

### 部署
- `deploy/auto-pull.ps1`：一鍵拉版、重啟、健檢失敗自動回退；`-Install` 可註冊排程（目前未啟用排程）
- 執行期資料（資產模型、評註、圖組、log）已全部移出版控

---

## 記法（給改動的人）

1. 每次 push 到 `main` 前，在 `[Unreleased]` 底下加一條：使用者看得到的差別，一句話
2. 要出版本時：把 `[Unreleased]` 改成 `[x.y.z] — 日期`、同步改 `VERSION`、commit 訊息 `release: x.y.z`
3. 修 bug 與文案 → 修訂版（0.9.1）；新功能 → 次版本（0.10.0）；資料格式或 API 不相容 → 主版本

## [0.23.0] — 2026-08-21

### 新增
- **API 金鑰／服務帳號**（`/keys`）：平台原本所有 API 都靠瀏覽器的登入 cookie，程式與 AI 代理根本連不進來——「外部模型用同一支 API 送結果進來」「客戶的程式來拿資料」這兩件事技術上做不到。現在每個人都能自己發金鑰：
  - **金鑰＝身分**，綁在你的帳號底下，看得到的圖面與資料跟你本人登入時完全一樣（test123 發的金鑰一樣只看得到 M0200）
  - **權限兩級**：唯讀／讀＋承接結果。改點位綁定、改資產模型這類**人工簽名過**的資料不開放程式覆寫
  - 明碼只顯示一次（平台只存雜湊）；可隨時撤銷；每把記最後使用時間與呼叫次數；金鑰本身不能用來管帳號或再發金鑰
- **MCP 端點**（`/mcp`）：**客戶接自己的 AI**，雲端（Claude／GPT）或地端（NDA 場域用地端模型，圖面與數據不出廠）都行，平台不綁 AI 供應商。Claude Desktop／Code 填 URL＋金鑰即可連
  - 12 個工具：圖面清單、資產模型、製程順序、製程說明、現場評註、資料集、**點位語意**（每欄對到哪台設備／量測項／單位／合理範圍／誰簽的）、統計、時序、既有建議、原始檔下載，以及 `post_result` 承接結果
  - 時序一次最多 2,000 點且一定標明已抽樣——**平台不當資料管線**，大量計算請下載原始檔在本地做完，再把結果送回來
  - 地端模型多半不講 MCP：同一份工具契約由 `/mcp/openai-tools` 吐成 OpenAI function calling 格式，`/mcp/call` 直呼
- `/keys` 頁面直接給 Claude 設定檔、地端模型 curl 範例、REST 範例，以及這把金鑰能用的工具清單

## [0.22.1] — 2026-08-21

### 修正
- 點位對照的「系統猜的（10）／把 10 個高信心猜測全部確認並簽名」看不懂，整段重寫：改成「**自動對應待確認（10 欄）**」，並直接說明**高信心＝欄名開頭就是資產庫裡的位號**；旁邊 ? 說明把握度怎麼算（前綴完全相同 90%、第一段是位號 85%、位號只在欄名中間 60% 不列入）。原本那行沒頭沒尾的「209×4、210×3、212×3」改成可展開清單，逐欄列出「欄名 → 對到哪台設備・子部件・量測項」與各自的把握度

## [0.22.0] — 2026-08-21

### 新增
- 圖上的框**依 AI 用法上色**（跟右邊徽章同一套）：藍＝最佳化、紫＝虛擬量測、琥珀＝品質預測；還沒算出建議的用虛線灰框，算完才轉實線。聚光燈徽章上也標了用法。工具列右側加圖例
- **製程說明搬進來了**（判讀工作台 ④ 產生的那一份），畫布下方可開可關（工具列「製程說明」／面板右上「關閉」，選擇會記住）；導覽到哪一台，說明裡提到它的位號就標起來並捲過去，標題列顯示「提到 209：3 處」與這份說明的產生時間／是否已確認

## [0.21.1] — 2026-08-21

### 修正
- 製程建議：**點圖上空白處＝取消選取**（回總覽）；拖曳平移不會誤觸
- 製程建議的資產模型**預設不畫流向箭頭**（那些線在這一頁是雜訊），工具列多一顆「流向箭頭」要看再開，選擇會記住
- 鏈上設備若在圖上沒有框（資產庫只剩清冊列）不再空白：退回顯示整條鏈，並提示去判讀工作台補框

## [0.21.0] — 2026-08-20

### 新增
- **現場實際值的來源可以指定與手改**：那一塊不再只是文字，改成「現場值 / 模型值 / 差」＋出處與連線狀態，按「設定來源」可選 **平台歷史資料／人工輸入（離線化驗）／OPC UA／Modbus TCP／MQTT／REST API**，依協定填點位位址（OPC UA 填 Endpoint＋NodeId、Modbus 填 Host/Port/Unit/位址/倍率…），人工輸入可直接覆寫目前值。目前存的是綁定設定與人工值，即時連線讀值是下一步
- **最佳化怎麼調的寫出來**：新增「怎麼找出這個建議的」——調整對象、可動範圍（取自歷史 P1～P99，不外推）、找什麼（壓到最低／逼近規格上限）、釘住不動的條件（上游承接來的）、試了幾組（隨機 3,000 組粗掃 → 前 50 名各再細搜 40 組）

## [0.20.1] — 2026-08-20

### 修正
- 模型卡全面改講人話，術語一律收進右側 **?**（滑鼠移過去看定義）：「R² 0.98589」→「解釋力 98.6%」、「RMSE」→「平均誤差 ±0.05 %」、「5 折交叉驗證」→「切 5 份輪流當考卷」、「殘差／容許帶」→「差多少（容許 ±0.10）」、「X 因子檢查點」→「這條建議還算數嗎」。三種任務類型（最佳化／虛擬量測／品質預測）旁也各有一個 ? 解釋用途
- **X 因子只列主要的**：實務上能拿到的欄位會全部丟進去訓練，列全部沒有意義——改成依貢獻度（permutation importance）排序列前幾個並畫佔比條，底下註明「共 N 個欄位進入訓練」

## [0.20.0] — 2026-08-20

### 新增
- 製程建議改成**導覽模式**：一次只講一台，圖上聚光燈只亮它（框外壓暗）並自動 pan／zoom 過去，左右鍵或 ‹ › 走 ①②③；可切「總覽」看全鏈與三段合計
- 一條鏈示範**三種 AI 用法**，每一步明標是哪一種：**① 209 最佳化 → ② 210 虛擬量測 → ③ 212 品質預測**（210 出料含水廠內沒有量測儀，只能靠離線化驗，正是虛擬量測的用途）
- 每一步展開完整**模型卡**：Y 用什麼建的（演算法／R²／RMSE／驗證切分／X 因子清單）、預測值 **±95% 區間**、**實際 vs 回傳**（現場實際值 ↔ 同一刻 X 餵模型的回傳值 ↔ 殘差是否在容許帶內＝模型還準不準）、**X 因子檢查點**（建議當下值／現在值／±2σ 容許帶，偏離就代表建議前提已變該重算）
- 承接 API 加 `task` 與 `detail` 欄位——外部模型也能一起帶模型卡資料進來

### 修正
- 圖上不再畫穿過設備的曲線、不再重複疊設備名稱（圖本來就寫了），改用聚光燈＋一個「現行 → 建議」徽章

## [0.19.1] — 2026-08-20

### 修正
- 製程建議改成**真資產模型**當畫面主體：左半直接畫潤泰 M0200 礦化及造粒的資產模型（可切盲重建／標示／描圖、可縮放平移），鏈上三台是圖上真的框——**209 捏和擠出機 → 210 隧道式烘箱帶運機 65M → 212 雙軸破碎機**（這張圖真實的物料順序），框旁直接標建議值、框與框之間畫承接線；右半是起承轉合三張卡。頂端明標「資產模型：真實（M0200）｜點位數值：模擬」
- 原本的 901／903／905 是照潤泰的線抄卻掛假號碼，看起來像潤泰但號碼全錯——整組換掉
- 第三段改成破碎機：承接烘箱的含水 0.8%，料不再過乾＝不易碎成細粉，破碎頻率可提高（產能上去），細粉率仍守規格 ≤2.0%（「細粉回料」是這張圖上真的有的東西）

## [0.19.0] — 2026-08-20

### 新增
- **製程建議**（`/twin/advisor`）＋**模型結果承接 API**（`POST /api/twin/result`）：任何模型（平台內建 AutoML、客戶自己的、第三方）算出的結果都能落到指定資產上，記「誰算的、根據哪個點位、什麼時候、承接自哪個上游結果」。平台內建的 AutoML 走同一條 API，不開後門
- **三段連鎖最佳化示範**（虛構三設備小廠，與客戶資料完全隔離）：一鍵跑「A 擠出機找最省電轉速 → 最佳解沿製程鏈固定成 B 烘箱的條件 → 再傳到 C 燒結窯」，三張建議卡標明彼此的承接關係，箭頭亮起表示條件已往下游傳；頁面附上每張卡對應的 API 呼叫內容。假數據 1 分鐘 × 92 天、埋真實物理耦合（電流對轉速呈 U 形、含水隨爐溫降、窯溫需求隨進料含水升），最近一週刻意做成操作漂移＝現行條件
- 首頁「組孿生」第 4 步改指製程建議；功能清單與引導列（輸入「建議／最佳化／承接」）都能到

## [0.18.1] — 2026-08-20

### 修正
- 文案再砍一輪（正式場合用）：審核卡按鈕改「存入資產庫／判讀有誤／重畫框」、動作說明段落全拿掉（收進 tooltip）；完成卡剩一句；「既有建檔 72 筆｜曾否決 15」一行、規則進 tooltip；階段標題拿掉副標；檢視器與說明卡的教學句全部收斂成一行以內

## [0.18.0] — 2026-08-20

### 新增
- **測試帳號（本機密碼登入）**：登入頁多了帳號密碼表單，不用 Google 也能進。帳號可設「只能看指定圖面」——test123 只看得到 M0200 礦化及造粒一張圖（清單、底圖、API 全擋，也不能上傳或刪圖）；密碼存雜湊不存明碼，可在權限管理停用
- 資料工作台多了「潤泰_三盤合併_1min.csv」：218／314／415 三盤同一時間軸的合併版（146,514 列 × 104 欄），點位對照選這一份就能把三盤的欄位全綁到同一張圖

### 修正
- 新版面 ①～④ 的說明文字全面精簡（商用場合不要一堆廢話）；製程說明卡按鈕改「確認這版」

## [0.17.0] — 2026-08-20

### 新增（新版面 /twin/pid/label2）
- 頂列**上一動／重做**＋**上次儲存時間**：按鈕上標還能退幾步、能重做幾步（Ctrl+Z／Ctrl+Shift+Z 也行）；「上次儲存 01:36:28（剛剛）」隨每次寫入更新。上一動改為游標式——可以連退多步、再重做，不再一退一進打轉；退了幾步後有新寫入，那幾個「未來」就放棄（稽核仍留在 `_abandoned/`）。目前只管資產庫（審核紀錄）；評註、點位各有自己的紀錄
- ④ 多一張**製程說明卡**：一顆「AI 產生預設製程說明」（依全部已確認元件＋整張圖），產生後顯示版本資訊與「重新產生／提修正意見／**確認這版說明**」；確認記誰、何時，重新產生或修訂＝新的一版，確認歸零——AI 寫的是預設稿，人確認了才算

### 修正
- 舊版面的「回到上一動」改走同一套游標式（不再一退一進打轉），歷史建檔清單標出目前是哪一版

## [0.16.1] — 2026-08-20

### 新增（新版面 /twin/pid/label2）
- ③ 檢視器的上下游可以**自己指定**：「＋ 上游／＋ 下游」後點圖上的元件（或框選它）就接上，每條連線旁 × 可刪；人工指定的連線寫進覆寫層，重建圖與順序圖跟著更新，重建模型不會洗掉
- ④ 點圖上既有的框＝直接對那個元件留評註（之前只能框選空白處）
- ④ 元件 ↔ 製程說明互動：選中元件列出說明裡提到它的句子，每句可「跳到說明處」（捲過去並反白）或「這句寫錯 → 提意見」（自動帶句子進修正意見框）；沒選元件時顯示「說明提到 M／N 個已確認位號」與沒提到的清單（點了跳圖上）；說明裡的位號點了會選中圖上元件

### 修正
- ① 的說明補上「沒全掃到沒關係：漏的到 ② 框選補、錯的審核時否決」
- 「台帳」字眼全部改掉：匯出鈕改「匯出設備清單 CSV」、同名重傳提示改「審核紀錄」
- ③④ 點元件不再觸發審核卡用的雲端「情境判讀／安裝別判定」（那是 ② 的事，省 token）

## [0.16.0] — 2026-08-20

### 新增
- **判讀工作台新版面（階段式）**先上 `/twin/pid/label2`，與舊版並存一週（舊版工具列「新版面 →」、新版右上「切回舊版」互通，帶同一張圖）。
  - 上方步驟列 ①辨識 ②審核 ③資產模型 ④評註與說明 ⑤點位對照（跳現有頁）；每步帶計數（候選數／審核進度／設備數／評註數），隨時可點回；① 有候選進來自動到 ②，其餘按「下一步 →」
  - 左欄拿掉：圖面切換／上傳／圖組收進步驟列最左「圖面 ▾」，畫布寬 224px
  - 右欄只放當前階段的東西；框選意義隨階段固定：①②③ 框選＝新增元件、④ 框選＝留評註；審核卡仍可順手「加評註」
  - ③ 右欄改「檢視器」：點左圖任一元件看它的框（可重框／加評註／刪除）、證據、評註、點位（平均值／單位／是否簽名）、上下游；沒選中顯示模型統計；資產庫清單收在可展開區；進 ③ 自動並排看模型
  - 製程說明只在 ④ 出現（畫布底下展開），①②③ 畫布拿回高度
  - 分區檢視／區域問答／雙引擎比對／引擎選擇收進右上「進階 ▾」

## [0.15.2] — 2026-08-20

### 修正
- 判讀工作台右欄「歷史建檔」移到最底（全部項目之後）——回到上一動是偶爾才用的事，不該擋在清單前面

## [0.15.1] — 2026-08-20

### 修正
- 判讀工作台點圖上空白處＝取消目前選取：聚光燈、藍框、作用區域、框選打開的表單一起收掉（點在框上不受影響；重框中不動目前項目）

## [0.15.0] — 2026-08-20

### 修正
- **框選後填了位號卻沒進資產庫**：以前框選會同時開「手動標註」和「評註」兩張表，填了評註那張「這是什麼＝211.4」就以為元件建好了——其實只留了一則評註，資產模型與點位對照裡都沒有 211.4。現在框選只開一張表：「這是什麼」填了像位號的東西（211.4／PI 65103／V-612）且資產庫沒有它，會自動出現「☑ 同時加入資產庫為〔設備▼〕」（預設勾選）；存檔＝這個框成為已確認的元件，評註欄留白就不另外生評註。資產庫已有的位號只留評註
- 既有的「只取了位號」評註（像你的 N1 211.4／N2 202.4／N3 304.2）卡片上多一顆「加入資產庫為 211.4（設備）」，一鍵升格；有留言的改掛到該元件、沒留言的直接收掉

## [0.14.4] — 2026-08-20

### 修正
- 開圖不再每次重跑「判讀圖面體系」：它底下是全頁 OCR（本機，潤泰 M0200 實測 15 秒），結果只跟 PDF 內容有關，現在落盤快取，第二次起 0.01 秒；同名重傳新檔自動失效重算。錨點層（向量幾何）同樣快取。開圖現在全部是讀檔，不呼叫任何雲端 AI

## [0.14.3] — 2026-08-20

### 修正
- 評註可以只取名不留言：框起來填「這是什麼」（例 211.4）就能存，不再跳「請先寫下評註內容」；兩格都空才擋。只取名的評註，製程說明會當作「現場標示此處為 211.4」引用

## [0.14.2] — 2026-08-20

### 修正
- 資產模型上的評註標籤不再寫「N3」：框選完多一格「這是什麼？」讓使用者自己取名（框裡有位號會先帶位號，沒有就自己打，例「停用管段」），資產模型（盲重建／描圖）的標籤、評註卡、圖上標記、製程說明的出處都顯示這個名字；既有評註點卡片上的名字即可改名，模型側立刻跟著換；沒取名的舊評註退回顯示框裡位號，再沒有才顯示流水號

## [0.14.1] — 2026-08-20

### 修正
- 框選區評註看不出框到誰：框住 211.4 留的評註卡片只寫「框選區」。現在框選區會自動帶出裡面的位號（「框選區 · 211.4」，多個列前 3 個 +N），新增時評註對象也先顯示「此框選區域（含 211.4）」；舊評註照現在的資產庫算、圖上標記與製程說明的出處標也一致；製程說明 RAG 把「框選區，含元件 211.4」餵給模型

## [0.14.0] — 2026-08-19

### 新增
- 點位對照**綁定要簽名**：按「確認並簽名」才算數，點位卡顯示「簽名 誰 · 時間」；只綁還沒確認的標「尚未簽名」；改了內容再存＝重新簽名（沒改不重簽）。整份對照留一條只增不刪的紀錄（誰、何時、把哪欄綁到哪台／確認／解綁／忽略），右側面板底部「紀錄」可展開
- 圖上每台已綁數據的設備旁多一張**數據狀態小表**：列各點位的平均值與單位（已確認綠字、猜的琥珀），超過 4 個摺成「＋N」；右上「數據狀態」按鈕可整批開關，記住選擇

### 修正
- 有綁數據的設備方塊變明顯：邊框加粗、填色拉高、外圈描白邊，在標示模式一堆綠框的底圖上也分得出來；位號標籤跟著上色
- 點位對照右上的連結文字改為「回到 P&ID 判讀工作台」

## [0.13.1] — 2026-08-19

### 修正
- 潤泰 SCADA 原始匯出（23／25／26／28.csv）上傳失敗「UnicodeDecodeError」——檔案是 Big5（cp950），上傳現在 UTF-8／Big5 都吃；單檔上限 50MB → 200MB（25.csv 96MB 實測 6 秒進庫）；「日期」＋「時間」分兩欄自動合併成 time（原本只抓到日期，整天同一個時間戳）

## [0.13.0] — 2026-08-19

### 新增
- **資料盤點**（`/data/inventory`）：一頁看完手上每份資料的年份、涵蓋期間、取樣間隔、覆蓋率與斷點；甘特時間軸一眼看出哪兩份資料期間不重疊（不重疊就不能直接 join）。認得民國年與「日期＋時間」兩欄格式
- 點位對照第②步可直接拖放上傳 CSV／Excel（走資料工作台同一個端點，上傳後自動選中並跳到③綁點位）
- 點位對照右上「改框選 → 判讀工作台」：圖上沒框到／框不準，一鍵帶著同一張圖過去（直接開圖、切並排看資產模型）；工作台左上多「← 回點位對照」，改完框回來還是同一張圖、同一份數據，不用重選

### 部署
- 自動追版排程改經 VBS 隱形啟動不再閃視窗；目前**停用**，改完手動跑 `deploy\auto-pull.ps1`

## [0.12.1] — 2026-08-18

### 修正
- 點位對照改為圖優先：①選資產模型（圖直接秀出來，可切標示／盲重建／描圖、可縮放）→ ②選歷史數據 → ③把欄位拖到圖上設備方塊綁定。設備方塊依數據狀態上色（綠＝有已確認點位／琥珀＝只有猜的／灰＝沒數據）並掛點位數；點方塊右側列它的量測點卡（統計、量測項、單位、子部件、合理範圍、確認）；未歸戶欄位為可拖曳 chip

## [0.12.0] — 2026-08-18

### 新增
- **點位對照**（`/twin/mapping`）：把歷史數據的每一欄對到 P&ID 設備，確認量測項、單位、子部件（M20 這類馬達迴路）、合理範圍——綁定孿生前的前處理。每欄自動算統計（平均／最小／最大／p1～p99／缺值率／迷你分佈圖）；欄名自動猜設備與量測項（潤泰 314 盤：20 欄猜中 16、信心 90%）；合理範圍預設帶 p1～p99 標「系統推定」，人改標「人工設定」；欄位視角與設備視角兩種切法；設備視角點欄位看 3 個月趨勢；高信心猜測可一鍵全部確認
- 首頁「組孿生」改四步：上傳歷史數據 → 判讀 P&ID → 點位對照 → 綁定孿生；功能清單與引導列（輸入「對照／點位／欄位」）都能到
- 資料工作台每個資料集卡「點位對照」入口；判讀工作台資產庫分頁「下一步：點位對照」入口

## [0.11.1] — 2026-08-18

### 移除
- 情境流量傳遞（起點入料量靜態傳遞）——方向不對，整組移除；下一步改為「點位對照」（先接歷史數據、確認點位對照後才綁定孿生）

## [0.11.0] — 2026-08-18

### 新增
- 資產庫清單每列右上 ×（可從資產庫刪除，走同一條確認＋稽核）；同位號同框的重複標「重複 ×N」
- 判讀工作台熱鍵 **Delete／Backspace**：刪掉目前選中的框（會先確認；已入庫留稽核可回到上一動、模型同步拿掉；候選等同否決）
- 首頁快捷 chip「P&ID 判讀」與功能清單「P&ID 判讀工作台」直達 `/twin/pid/label`；引導列輸入「判讀／標示／審核／框選」也會帶過去

## [0.10.1] — 2026-08-18

### 修正
- 首頁淺色模式下帳號選單是黑底深字（看不見）——彈窗底色改隨主題

## [0.10.0] — 2026-08-18

### 新增
- **首頁換新**：純白底＋即時 3D 廠區主視覺（虛線框標出儲槽／精餾塔／反應器／加熱爐／管架），右上角可切深色；中英雙版（選單切換）。舊首頁保留在 `/static/home.html`
- 首頁「前往」引導列命中即跳轉；帳號選單（登入者／角色／權限管理／登出）與頁尾版號都在新首頁上

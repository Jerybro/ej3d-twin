# EJ_3D 數位孿生平台（J.S Process Intelligence）— Python 依賴
# 由實測環境盤點鎖版（2026-07-05；2026-08-12 換機重建補齊缺漏並重驗）。
# 安裝：pip install -r requirements.txt
# Python 3.13（x64）驗證通過。Windows ARM64 機器也裝 x64 版 Python（走模擬），
# 科學運算套件的鎖定版缺 arm64 wheel，原生 ARM64 Python 會在 pip 階段失敗。

# --- 核心：Web 框架 + 檔案上傳 ---
fastapi==0.128.8
uvicorn==0.40.0
starlette==0.52.1
pydantic==2.12.5
python-multipart==0.0.32       # UploadFile / 表單上傳（缺→上傳端點報錯）
itsdangerous==2.2.0            # SessionMiddleware 簽章（缺→server.main 匯入即失敗）
websockets==17.0.1             # uvicorn WS 協定實作（缺→/ws 握手退化成 GET 回 404，孿生即時數據斷線）

# --- 資料工作台 + AutoML 建模 ---
numpy==2.2.6
pandas==2.3.1
scipy==1.16.0
scikit-learn==1.9.0
joblib==1.5.3
pyarrow==24.0.0                # dataprep 以 parquet 存資料集（缺→/api/data/upload 500）
openpyxl==3.1.5                # 讀寫 .xlsx（Excel 上傳與匯出）

# --- P&ID 判讀（pid_parse/pid_vlm/pid_flow…，延遲載入：開圖面才炸）---
pypdfium2==5.12.1              # PDF 渲染底圖（缺→上傳後「圖面載入失敗」）
easyocr==1.7.2                 # 位號 OCR＋倒置判定（拉 torch/torchvision，CPU 亦可跑）
networkx==3.6.1                # 管網拓撲／製程順序圖／資產模型

# --- 資料工作台建模 ---
statsmodels==0.14.6            # automl 時序統計（延遲載入）

# --- 離線工具（tools/train_surrogate.py 等，伺服器本身不需要）---
matplotlib==3.10.3

# --- 選用：廠端即時數據源（sources.py，lazy import；未接真數據可略）---
asyncua==1.1.8                 # OPC UA client
# pymodbus>=3.6                # Modbus TCP（目前未安裝）

# --- 選用：資料工作台物性模組 /api/data/props/*（目前未安裝）---
# CoolProp>=6.6
